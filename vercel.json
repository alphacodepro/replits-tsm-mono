{
  "installCommand": "npm install",
  "buildCommand": "echo \"VITE_API_URL=$VITE_API_URL\" > client/.env && npx vite build",
  "outputDirectory": "dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
