# =======================
# BACKEND ENVIRONMENT VARIABLES (Render)
# =======================

# Database Connection String (from Neon)
# Get this from your Neon dashboard: https://console.neon.tech
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Session Secret (generate a random string for production)
# You can generate one using: openssl rand -base64 32
SESSION_SECRET=your-secure-random-session-secret

# Frontend URL (your Vercel deployment URL)
# Example: https://yourapp.vercel.app
FRONTEND_URL=https://yourapp.vercel.app

# Node Environment (set to production on Render)
NODE_ENV=production

# Port (Render sets this automatically, default is 5000)
PORT=5000

# =======================
# FRONTEND ENVIRONMENT VARIABLES (Vercel)
# =======================

# Backend API URL (your Render deployment URL)
# Example: https://yourapp.onrender.com
VITE_API_URL=https://yourapp.onrender.com

# =======================
# DEPLOYMENT INSTRUCTIONS
# =======================

# 1. NEON DATABASE (Already set up ✓)
#    - Your database is already configured
#    - Use the same DATABASE_URL for Render

# 2. RENDER (Backend Deployment)
#    - Connect your GitHub repo
#    - Set Environment Variables:
#      * DATABASE_URL (from Neon)
#      * SESSION_SECRET (generate new)
#      * FRONTEND_URL (your Vercel URL once deployed)
#      * NODE_ENV=production
#    - Build Command: npm install && npm run build
#    - Start Command: npm start
#    - Deploy and copy your Render URL

# 3. VERCEL (Frontend Deployment)
#    - Connect your GitHub repo
#    - Framework Preset: Vite
#    - Root Directory: . (leave as default)
#    - Build Command: (uses vercel.json - leave as default)
#    - Set Environment Variables:
#      * VITE_API_URL (your Render backend URL)
#    - Deploy!

# =======================
# LOCAL DEVELOPMENT (Replit)
# =======================

# For local Replit development, only set:
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
# SESSION_SECRET is optional (has default)
# FRONTEND_URL is optional (defaults to localhost)
